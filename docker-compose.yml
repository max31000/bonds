version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: bonds-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-bonds_dev}
      MYSQL_DATABASE: bonds
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - bonds-network

  api:
    build:
      context: .
      dockerfile: src/Bonds.Api/Dockerfile
    container_name: bonds-api
    restart: unless-stopped
    ports:
      - "5001:5001"
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ASPNETCORE_URLS: http://+:5001
      ConnectionStrings__DefaultConnection: "Server=mysql;Database=bonds;User=root;Password=${MYSQL_ROOT_PASSWORD:-bonds_dev};AllowPublicKeyRetrieval=true;"
    volumes:
      # Персист ключей DataProtection на именованный volume — локальный цикл (`docker compose down`
      # без `-v` + `up`) ведёт себя как прод (plan/13 часть A): пересоздание контейнера api не рвёт
      # расшифровку токена T-Invest, сохранённого в БД через PUT /api/settings/tinvest-token.
      - dataprotection_keys:/app/dataprotection-keys
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - bonds-network

volumes:
  mysql_data:
  dataprotection_keys:

networks:
  bonds-network:
    name: bonds-network
    # external: true  # на VDS bonds-api подключается к сети общего MySQL-контейнера — см. README "Деплой — TODO"
