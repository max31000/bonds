-- Чистка после бага единиц цены (продакшн): LiveQuotesPollingService и BondSyncService часть 3
-- трактовали котировку T-Invest marketdata (TInvestQuote.LastPrice) в пунктах (% от номинала) как
-- рубли и писали её напрямую в intraday_quotes.dirty_price_rub - все существующие тики занижены
-- примерно в 10 раз (см. Bonds.Infrastructure.Quotes.LiveQuoteConverter, добавленный этой же
-- правкой). Таблица короткоживущая (retention 8 дней, plan/16 часть A) - потеря исторических тиков
-- допустима и согласована. Новые тики после этой миграции пишутся уже в правильных единицах.
-- Примечание для авторов будущих миграций: MigrationRunner делит файл на операторы по символу
-- точка-с-запятой без учёта SQL-комментариев - не используйте этот символ внутри строк-комментариев.
DELETE FROM intraday_quotes;
